import React, { Component } from 'react';
import SearchPresenter from '~/screens/Search/SearchPresenter';

class SearchContainer extends Component {

    render(){
        return (
            <SearchPresenter />
        )
    }
};

export default SearchContainer;